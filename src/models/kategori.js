const dbPool = require("../config/database.js");

const getAllKategori = () => {
  const SQLQuery = ``;
  return dbPool.execute(SQLQuery);
};
const creatKategori = () => {
  const SQLQuery = ``;
  return dbPool.execute(SQLQuery);
};
const updateKategori = () => {
  const SQLQuery = ``;
  return dbPool.execute(SQLQuery);
};
const deleteKategori = () => {
  const SQLQuery = ``;
  return dbPool.execute(SQLQuery);
};
const getAllKategoriByID = () => {
  const SQLQuery = ``;
  return dbPool.execute(SQLQuery);
};
